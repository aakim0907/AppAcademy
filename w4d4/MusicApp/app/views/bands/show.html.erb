<br>
<%= link_to "<< Back to Albums", bands_url %>

<h2> <%= @band.name %> </h2>
<h3>Album list:</h3>

<% if @band.albums.empty? %>
  <p>No albums yet! Add an album to the list!</p>
<% else %>
  <ul>
  <% @band.albums.each do |album| %>
    <li> <%= link_to album.name, album_url(album) %> </li>
  <% end %>
  </ul>
<% end %>

<%= button_to "Edit Band", edit_band_url(@band), method: :get %>
<%= button_to "Delete Band", band_url(@band), method: :delete %>
